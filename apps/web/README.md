# @glfonline/web

GLF Online web storefront (React Router v8, Vite SSR).

## Development

From the repository root:

```sh
pnpm dev:web
```

## Deployment

Production deploys to Netlify. Set the Netlify **Package directory** to `apps/web`.
